version: "3.8"
services:
  db:
    image: mysql:8.0
    environment:
      MYSQL_ROOT_PASSWORD: rootpassword
      MYSQL_DATABASE: bike-rent
      MYSQL_ROOT_HOST: "%"
      MYSQL_USER: avnadmin
      MYSQL_PASSWORD: garek123
      TZ: Europe/Warsaw
    ports:
      - "3307:3306"
    volumes:
      - ./init.sql:/docker-entrypoint-initdb.d/init.sql
